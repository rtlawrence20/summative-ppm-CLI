# Project Management CLI

A Python command-line interface (CLI) for managing a simulated multi-user project tracker. This app will allow users to create & manage users, projects, and tasks using local data persistence.

## Features

TBD

## Installation

1. Clone this repository
2. Install dependencies:
```bash
pip install -r requirements.txt
```